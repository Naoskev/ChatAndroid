<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mines_nantes.utilisateur.chat" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ui.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.Logged_activity"
            android:label="@string/title_activity_logged_activity" >
        </activity>
        <activity
            android:name=".ui.DrawActivity"
            android:label="@string/title_activity_draw" >
        </activity>
        <activity
            android:name=".ui.DashboardActivity"
            android:label="@string/title_activity_dashboard" >
        </activity>
        <activity
            android:name=".ui.SendMessageActivity"
            android:label="@string/title_activity_send_message" >
        </activity>
    </application>

</manifest>
